{
  "name": "payment-gateway",
  "private": true,
  "description": "Root package — dev tooling only (commitlint + husky)",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.0.0",
    "@commitlint/config-conventional": "^19.0.0",
    "husky": "^9.0.0"
  }
}
