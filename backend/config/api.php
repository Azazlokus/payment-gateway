<?php

declare(strict_types=1);

return [
    // API Key для X-Api-Key заголовка.
    // Пустая строка — проверка отключена (локальная разработка).
    //
    // Ротация ключей: укажи оба через запятую — API_KEY=old-key,new-key
    // Оба принимаются одновременно. После перевода клиентов на новый —
    // удали старый.
    'key' => env('API_KEY', ''),
];
